# Raw data

- `runs.jsonl`: newline-delimited JSON, one row per run.
- `benchmark_results_1_1.yaml`: model-level summary metrics.
- `build_data.py`: builds `data/` artifacts (runs pickle + counts tables).
